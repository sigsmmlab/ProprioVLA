\section{Preliminaries}

Define the VLA policy setting, observation and action spaces, robot
proprioceptive state, task instruction, and any notation used by the method.

\paragraph{Language-conditioned visuomotor control.}
Let \(x\) denote the language instruction, \(o_t\) the visual observation,
\(s_t\) the proprioceptive robot state, and \(a_t\) the robot action at time
\(t\).

\paragraph{Policy objective.}
State the supervised learning, imitation learning, or downstream control
objective used to train and evaluate the policy.
